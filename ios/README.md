AI Job iOS App — README

This folder contains a SwiftUI prototype. To run locally:
- Open in Xcode (add GoogleService-Info.plist as instructed)
- Install Firebase SDK via SPM
- Follow the main project README for backend endpoints (/api/parse-resume)

Full SwiftUI prototype (detailed in project repo) includes resume upload, OCR, and Firestore integration.
