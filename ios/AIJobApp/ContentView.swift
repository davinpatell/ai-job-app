// ContentView placeholder - full code available in project README
import SwiftUI
struct ContentView: View {
    var body: some View {
        Text("AI Job App - open README for full source")
            .padding()
    }
}
